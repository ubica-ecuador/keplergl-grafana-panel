# Copy to .env and fill in. Compose reads .env automatically and passes these
# through to the Grafana containers, where provisioning/ interpolates them.

# Password for the provisioned MobilityDB data source.
MOBILITYDB_PASSWORD=
