# Generated fixtures for the sources bench. Prettier and json.dumps disagree on
# how to lay out coordinate arrays, so formatting these would put every run of
# testdata/make-testdata.py permanently at odds with the committed files.
testdata/

# Managed by create-plugin; this project's constraints forbid modifying it, and
# regenerating it would discard any formatting applied here anyway.
/.config/

# Written by scripts/vendor-*.mjs, which must reproduce them byte for byte.
src/icons/temaki-paths.json
src/icons/ocha-paths.json
