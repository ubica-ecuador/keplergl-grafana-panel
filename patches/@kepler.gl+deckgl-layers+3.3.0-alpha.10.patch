diff --git a/node_modules/@kepler.gl/deckgl-layers/dist/cjs/raster/webgl/texture/mask.js b/node_modules/@kepler.gl/deckgl-layers/dist/cjs/raster/webgl/texture/mask.js
index 568c312..3d1ce33 100644
--- a/node_modules/@kepler.gl/deckgl-layers/dist/cjs/raster/webgl/texture/mask.js
+++ b/node_modules/@kepler.gl/deckgl-layers/dist/cjs/raster/webgl/texture/mask.js
@@ -7,7 +7,11 @@ exports.maskUint = exports.maskInt = exports.maskFloat = void 0;
 // SPDX-License-Identifier: MIT
 // Copyright contributors to the kepler.gl project
 
-var inf = Math.pow(2, 62);
+// PATCHED (keplergl/kepler.gl#3704): the "no bound" default. A tile server writes the
+// mask in the band's own data type and marks valid pixels with that type's maximum --
+// 255 for uint8, 65535 for uint16, FLT_MAX for float32 -- so a smaller default here
+// discards every valid pixel of a float32 mask.
+var inf = 3.4028234663852886e38;
 function getUniforms() {
   var opts = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
   var imageMask = opts.imageMask,
diff --git a/node_modules/@kepler.gl/deckgl-layers/dist/esm/raster/webgl/texture/mask.js b/node_modules/@kepler.gl/deckgl-layers/dist/esm/raster/webgl/texture/mask.js
index baa3fad..e6637d5 100644
--- a/node_modules/@kepler.gl/deckgl-layers/dist/esm/raster/webgl/texture/mask.js
+++ b/node_modules/@kepler.gl/deckgl-layers/dist/esm/raster/webgl/texture/mask.js
@@ -1,7 +1,11 @@
 // SPDX-License-Identifier: MIT
 // Copyright contributors to the kepler.gl project
 
-var inf = Math.pow(2, 62);
+// PATCHED (keplergl/kepler.gl#3704): the "no bound" default. A tile server writes the
+// mask in the band's own data type and marks valid pixels with that type's maximum --
+// 255 for uint8, 65535 for uint16, FLT_MAX for float32 -- so a smaller default here
+// discards every valid pixel of a float32 mask.
+var inf = 3.4028234663852886e38;
 function getUniforms() {
   var opts = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
   var imageMask = opts.imageMask,
