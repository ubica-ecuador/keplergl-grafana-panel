{
  "annotations": {
    "list": []
  },
  "description": "A temporal WMS as a map layer: the panel's clock walks the service's dates.",
  "editable": true,
  "id": null,
  "links": [],
  "preload": false,
  "refresh": "",
  "schemaVersion": 41,
  "tags": [
    "ubica",
    "wms",
    "raster"
  ],
  "templating": {
    "list": [
      {
        "name": "capa",
        "label": "Service layer",
        "type": "custom",
        "query": "persiann_pdir_24h,persiann_pdir_48h,persiann_pdir_72h,persiann_no_precipitation,imerg_early_run_24h",
        "current": {
          "selected": true,
          "text": "persiann_pdir_24h",
          "value": "persiann_pdir_24h"
        },
        "options": [],
        "includeAll": false,
        "multi": false,
        "description": "Qué capa del servicio se dibuja. Va a la columna wms_layer, así que el mapa la sigue sin editar nada."
      },
      {
        "name": "lluvia",
        "label": "Value under the click",
        "type": "textbox",
        "query": "",
        "current": {
          "selected": false,
          "text": "",
          "value": ""
        },
        "options": [],
        "description": "Lo escribe el mapa: pinchar sobre la capa pregunta al servicio por ese píxel, en la fecha que se está viendo."
      },
      {
        "name": "area",
        "label": "Drawn area",
        "type": "textbox",
        "query": "",
        "current": {
          "selected": false,
          "text": "",
          "value": ""
        },
        "options": [],
        "description": "La escribe el mapa al dibujar un polígono, como WKT. Vacía significa Ecuador entero."
      },
      {
        "name": "mapFrom",
        "label": "Map window · from",
        "type": "textbox",
        "query": "",
        "current": {
          "selected": false,
          "text": "",
          "value": ""
        },
        "options": [],
        "hide": 2,
        "description": "La escribe el mapa al mover su barra de tiempo."
      },
      {
        "name": "mapTo",
        "label": "Map window · to",
        "type": "textbox",
        "query": "",
        "current": {
          "selected": false,
          "text": "",
          "value": ""
        },
        "options": [],
        "hide": 2,
        "description": "La escribe el mapa al mover su barra de tiempo. Es la fecha que el análisis zonal usa."
      },
      {
        "name": "west",
        "label": "Viewport · west",
        "type": "textbox",
        "query": "",
        "current": {
          "selected": false,
          "text": "",
          "value": ""
        },
        "options": [],
        "hide": 2,
        "description": "La escribe el mapa al moverse. Sirve de zona cuando no hay polígono dibujado."
      },
      {
        "name": "south",
        "label": "Viewport · south",
        "type": "textbox",
        "query": "",
        "current": {
          "selected": false,
          "text": "",
          "value": ""
        },
        "options": [],
        "hide": 2,
        "description": "La escribe el mapa al moverse. Sirve de zona cuando no hay polígono dibujado."
      },
      {
        "name": "east",
        "label": "Viewport · east",
        "type": "textbox",
        "query": "",
        "current": {
          "selected": false,
          "text": "",
          "value": ""
        },
        "options": [],
        "hide": 2,
        "description": "La escribe el mapa al moverse. Sirve de zona cuando no hay polígono dibujado."
      },
      {
        "name": "north",
        "label": "Viewport · north",
        "type": "textbox",
        "query": "",
        "current": {
          "selected": false,
          "text": "",
          "value": ""
        },
        "options": [],
        "hide": 2,
        "description": "La escribe el mapa al moverse. Sirve de zona cuando no hay polígono dibujado."
      }
    ]
  },
  "time": {
    "from": "now-45d",
    "to": "now"
  },
  "timepicker": {},
  "timezone": "utc",
  "title": "Rainfall — PERSIANN over a temporal WMS",
  "uid": "wms-time",
  "version": 1,
  "weekStart": "",
  "panels": [
    {
      "type": "text",
      "title": "",
      "gridPos": {
        "h": 7,
        "w": 24,
        "x": 0,
        "y": 0
      },
      "id": 1,
      "options": {
        "mode": "markdown",
        "content": "## Rainfall over a temporal WMS — PERSIANN over Ecuador\n\nThe service is [GeoGLOWS'](https://services.geoglows.org/geoserver/): a GeoServer with a `TIME` dimension, publishing one image per day and expecting a date to be asked for in the request itself.\n\n**Drag the map's time bar.** The image changes without querying again and without moving the viewport: the date travels in the GetMap's `TIME`. A drag that lands between two days asks for nothing. The query returns no images — it returns the service, the layer, and the dates the service itself says it has.\n\n**Click on the rain** and the panel asks the service what lies under that pixel, on the date you are looking at. The figure appears above, under \"Value under the click\", and any other panel can read it.\n\n**Draw a polygon** with the map's tool: the very data you see painted is read as numbers over WCS — the other face of the same GeoServer — and DuckDB clips it to your area. The WMS draws; the WCS hands over the pixels. No ETL and no second source — and the number follows the same time bar as the image.\n\nBelow, the **seven-day series** over that same area: with no polygon drawn it measures whatever you are looking at, so moving the map recomputes it."
      }
    },
    {
      "datasource": {
        "type": "motherduck-duckdb-datasource",
        "uid": "duckdb"
      },
      "description": "The query names the service and the layer; the map's clock picks the day. No tile server is involved: the WMS draws its own images.",
      "gridPos": {
        "h": 19,
        "w": 14,
        "x": 0,
        "y": 7
      },
      "id": 2,
      "type": "ubica-keplergl-panel",
      "targets": [
        {
          "datasource": {
            "type": "motherduck-duckdb-datasource",
            "uid": "duckdb"
          },
          "format": 1,
          "rawQuery": true,
          "rawSql": "-- Las fechas las publica el propio servicio, en su GetCapabilities: una capa\n-- con dimensión TIME lista ahí todas las pasadas que tiene. Así que la consulta\n-- no inventa un calendario, lo lee — y de paso nombra el servicio y la capa.\n--\n-- Nada de esto trae imágenes: un WMS las dibuja él mismo. La ventana del reloj\n-- del mapa elige la fecha, en el navegador, y viaja como TIME en el GetMap.\n--\n-- OJO con $capa: este datasource entrecomilla las variables por su cuenta, así\n-- que va desnuda y concatenada. Metida dentro de unas comillas se convertiría\n-- en ''persiann_pdir_24h'' y la consulta no llega ni al parser.\nWITH caps AS (\n  SELECT http_get(\n    'https://services.geoglows.org/geoserver/satellite_based_precipitation/wms'\n    || '?service=WMS&request=GetCapabilities'\n  )->>'body' AS xml\n), extent AS (\n  SELECT regexp_extract(\n    xml,\n    '(?s)<Name>' || $capa || '</Name>.*?<Dimension name=\"time\"[^>]*>([^<]*)</Dimension>',\n    1\n  ) AS times\n  FROM caps\n), fechas AS (\n  -- En su propia etapa: DuckDB no admite UNNEST en un SELECT que además filtra.\n  SELECT unnest(str_split(times, ','))::TIMESTAMPTZ AS time FROM extent\n)\nSELECT\n  time,\n  'https://services.geoglows.org/geoserver/satellite_based_precipitation/wms' AS wms_url,\n  $capa AS wms_layer\nFROM fechas\nWHERE time BETWEEN $__timeFrom()::TIMESTAMPTZ AND $__timeTo()::TIMESTAMPTZ\nORDER BY time\n",
          "refId": "A"
        }
      ],
      "options": {
        "showSidePanel": false,
        "timeSync": "variables",
        "mapConfig": {
          "version": "v1",
          "config": {
            "mapState": {
              "latitude": -1.7,
              "longitude": -78.6,
              "zoom": 5.4,
              "bearing": 0,
              "pitch": 0,
              "dragRotate": false
            }
          }
        },
        "variableMappings": [
          {
            "field": "wms_value",
            "variable": "lluvia",
            "source": "click"
          }
        ],
        "areaVariable": "area",
        "timeVariables": {
          "from": "mapFrom",
          "to": "mapTo"
        },
        "viewportVariables": {
          "west": "west",
          "south": "south",
          "east": "east",
          "north": "north"
        }
      },
      "title": "PERSIANN over Ecuador · drag the time, click the map"
    },
    {
      "datasource": {
        "type": "motherduck-duckdb-datasource",
        "uid": "duckdb"
      },
      "description": "The same thing that feeds the map, as a table: one row per available day.",
      "gridPos": {
        "h": 10,
        "w": 10,
        "x": 14,
        "y": 7
      },
      "id": 3,
      "type": "table",
      "targets": [
        {
          "datasource": {
            "type": "motherduck-duckdb-datasource",
            "uid": "duckdb"
          },
          "format": 1,
          "rawQuery": true,
          "rawSql": "-- Las fechas las publica el propio servicio, en su GetCapabilities: una capa\n-- con dimensión TIME lista ahí todas las pasadas que tiene. Así que la consulta\n-- no inventa un calendario, lo lee — y de paso nombra el servicio y la capa.\n--\n-- Nada de esto trae imágenes: un WMS las dibuja él mismo. La ventana del reloj\n-- del mapa elige la fecha, en el navegador, y viaja como TIME en el GetMap.\n--\n-- OJO con $capa: este datasource entrecomilla las variables por su cuenta, así\n-- que va desnuda y concatenada. Metida dentro de unas comillas se convertiría\n-- en ''persiann_pdir_24h'' y la consulta no llega ni al parser.\nWITH caps AS (\n  SELECT http_get(\n    'https://services.geoglows.org/geoserver/satellite_based_precipitation/wms'\n    || '?service=WMS&request=GetCapabilities'\n  )->>'body' AS xml\n), extent AS (\n  SELECT regexp_extract(\n    xml,\n    '(?s)<Name>' || $capa || '</Name>.*?<Dimension name=\"time\"[^>]*>([^<]*)</Dimension>',\n    1\n  ) AS times\n  FROM caps\n), fechas AS (\n  -- En su propia etapa: DuckDB no admite UNNEST en un SELECT que además filtra.\n  SELECT unnest(str_split(times, ','))::TIMESTAMPTZ AS time FROM extent\n)\nSELECT\n  time,\n  'https://services.geoglows.org/geoserver/satellite_based_precipitation/wms' AS wms_url,\n  $capa AS wms_layer\nFROM fechas\nWHERE time BETWEEN $__timeFrom()::TIMESTAMPTZ AND $__timeTo()::TIMESTAMPTZ\nORDER BY time\n",
          "refId": "A"
        }
      ],
      "title": "The days the service offers",
      "fieldConfig": {
        "defaults": {
          "custom": {
            "align": "auto"
          }
        },
        "overrides": []
      },
      "options": {
        "showHeader": true
      }
    },
    {
      "datasource": {
        "type": "motherduck-duckdb-datasource",
        "uid": "duckdb"
      },
      "description": "The same data the map draws, read over WCS and clipped in DuckDB. The area is whatever polygon you draw; with none, the map's viewport. The date is the one at the end of the map's time bar.",
      "gridPos": {
        "h": 9,
        "w": 10,
        "x": 14,
        "y": 17
      },
      "id": 4,
      "type": "stat",
      "targets": [
        {
          "datasource": {
            "type": "motherduck-duckdb-datasource",
            "uid": "duckdb"
          },
          "format": 1,
          "rawQuery": true,
          "rawSql": "-- El otro eje del mismo dato: el WMS lo pinta, el WCS lo entrega en números.\n-- GeoServer publica ambos sobre la misma capa, así que esto no es otra fuente\n-- ni otro ETL — es la misma escena, leída en vez de dibujada.\n--\n-- La fecha se la preguntamos al servicio, igual que el mapa: suponerla a partir\n-- del rango daría 404 el día que el rango va por delante de la última pasada.\nINSTALL raster FROM community; LOAD raster;\nSELECT RT_GdalConfig('CURL_CA_BUNDLE','/etc/ssl/certs/ca-certificates.crt');\n\n-- Sin polígono dibujado, la zona es Ecuador entero: el panel dice algo desde el\n-- primer momento, y dibujar en el mapa lo acota.\nSET VARIABLE zona = coalesce(\n  nullif($area, ''),\n  CASE WHEN nullif($west, '') IS NOT NULL THEN\n    'POLYGON((' || $west || ' ' || $north || ', ' || $east || ' ' || $north || ', '\n                || $east || ' ' || $south || ', ' || $west || ' ' || $south || ', '\n                || $west || ' ' || $north || '))'\n  END,\n  'POLYGON((-80 1, -77 1, -77 -3, -80 -3, -80 1))'\n);\n\nSET VARIABLE fecha = (\n  WITH caps AS (\n    SELECT http_get('https://services.geoglows.org/geoserver/satellite_based_precipitation/wms?service=WMS&request=GetCapabilities')->>'body' AS xml\n  ), extent AS (\n    SELECT regexp_extract(xml, '(?s)<Name>' || $capa || '</Name>.*?<Dimension name=\"time\"[^>]*>([^<]*)</Dimension>', 1) AS times FROM caps\n  ), fechas AS (\n    SELECT unnest(str_split(times, ','))::TIMESTAMPTZ AS t FROM extent\n  )\n  -- El reloj que manda aquí es el del mapa, no el del dashboard: el panel\n  -- responde a la barra de tiempo que estás arrastrando. `mapTo` la publica el\n  -- propio mapa (opción «Time variables»); vacía —antes del primer movimiento—\n  -- vale el extremo del rango del dashboard.\n  SELECT max(t) FROM fechas\n  WHERE t <= coalesce(try_cast(nullif($mapTo, '') AS TIMESTAMPTZ), $__timeTo()::TIMESTAMPTZ)\n);\n\n-- RT_Read exige una ruta literal en tiempo de bind, así que la URL se arma en\n-- una variable y se lee con getvariable(). El recuadro sale del polígono: se\n-- descarga la zona y no la escena entera.\nSET VARIABLE cobertura = (\n  SELECT '/vsicurl/https://services.geoglows.org/geoserver/satellite_based_precipitation/wcs'\n      || '?service=WCS&version=2.0.1&request=GetCoverage'\n      || '&coverageId=satellite_based_precipitation__' || $capa || '&format=image/tiff'\n      || '&subset=Long(' || round(ST_XMin(g), 3) || ',' || round(ST_XMax(g), 3) || ')'\n      || '&subset=Lat('  || round(ST_YMin(g), 3) || ',' || round(ST_YMax(g), 3) || ')'\n      || '&subset=time(%22' || strftime(getvariable('fecha') AT TIME ZONE 'UTC', '%Y-%m-%dT%H:%M:%S.000Z') || '%22)'\n  FROM (SELECT ST_GeomFromText(getvariable('zona')) AS g)\n);\n\n-- RT_CubeClip rellena con -99 fuera del polígono, que es justo lo que este\n-- producto ya usa para «sin dato», así que el agregado lo descarta solo. Nada\n-- de RT_CubeSetNoData: el fichero trae su nodata declarado y sobreescribirlo\n-- devuelve mínimos negativos en un producto de lluvia.\nSELECT\n  getvariable('fecha')::TIMESTAMPTZ AS time,\n  round((s).mean, 3)                AS \"media mm\",\n  (s).maximum                       AS \"máximo mm\",\n  (s).valid_count                   AS \"celdas en la zona\"\nFROM (\n  SELECT RT_CubeStats_Agg(\n           RT_CubeClip(databand_1, tile_x, tile_y, metadata, ST_GeomFromText(getvariable('zona')), -99.0), 0) AS s\n  FROM RT_Read(getvariable('cobertura'))\n);\n",
          "refId": "A"
        }
      ],
      "title": "Rain over the area · one day",
      "fieldConfig": {
        "defaults": {
          "unit": "none",
          "decimals": 2,
          "color": {
            "mode": "fixed",
            "fixedColor": "text"
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "celdas en la zona"
            },
            "properties": [
              {
                "id": "decimals",
                "value": 0
              },
              {
                "id": "displayName",
                "value": "cells in the area"
              }
            ]
          }
        ]
      },
      "options": {
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        },
        "textMode": "value_and_name",
        "colorMode": "none",
        "graphMode": "none",
        "orientation": "horizontal"
      }
    },
    {
      "datasource": {
        "type": "motherduck-duckdb-datasource",
        "uid": "duckdb"
      },
      "description": "The seven days ending today, over the polygon you draw — and with none, over the map's viewport. That is seven WCS requests, one per day: RT_Read demands a literal path, and a time range in WCS returns a single image.",
      "gridPos": {
        "h": 9,
        "w": 24,
        "x": 0,
        "y": 26
      },
      "id": 5,
      "type": "timeseries",
      "targets": [
        {
          "datasource": {
            "type": "motherduck-duckdb-datasource",
            "uid": "duckdb"
          },
          "format": 1,
          "rawQuery": true,
          "rawSql": "-- Serie de 7 días sobre la zona: el mismo dato que pinta el mapa, leído en\n-- números por WCS y recortado en DuckDB. Una petición por día, y no por pereza:\n-- RT_Read exige ruta literal en tiempo de bind, y un subset temporal con rango\n-- en WCS 2.0.1 devuelve una sola loncha, no un cubo. Así que 7 lecturas.\n--\n-- La zona es el polígono que dibujes; si no hay ninguno, el encuadre del mapa;\n-- y si el panel no publica encuadre, Ecuador entero.\nINSTALL raster FROM community; LOAD raster;\nSELECT RT_GdalConfig('CURL_CA_BUNDLE','/etc/ssl/certs/ca-certificates.crt');\n\nSET VARIABLE zona = coalesce(\n  nullif($area, ''),\n  CASE WHEN nullif($west, '') IS NOT NULL THEN\n    'POLYGON((' || $west || ' ' || $north || ', ' || $east || ' ' || $north || ', '\n                || $east || ' ' || $south || ', ' || $west || ' ' || $south || ', '\n                || $west || ' ' || $north || '))'\n  END,\n  'POLYGON((-80 1, -77 1, -77 -3, -80 -3, -80 1))'\n);\n\n-- Los siete días naturales que terminan hoy, cruzados con los que el servicio\n-- dice tener: uno que le falte —hoy mismo, hasta que publique la pasada— se\n-- queda sin punto, en vez de dar 404 y tumbar todo.\nSET VARIABLE dias = (\n  WITH caps AS (\n    SELECT http_get('https://services.geoglows.org/geoserver/satellite_based_precipitation/wms?service=WMS&request=GetCapabilities')->>'body' AS xml\n  ), extent AS (\n    SELECT regexp_extract(xml, '(?s)<Name>' || $capa || '</Name>.*?<Dimension name=\"time\"[^>]*>([^<]*)</Dimension>', 1) AS times FROM caps\n  ), fechas AS (\n    SELECT unnest(str_split(times, ','))::TIMESTAMPTZ AS t FROM extent\n  )\n  SELECT list(t ORDER BY t DESC)\n  FROM fechas\n  WHERE t >= current_date - INTERVAL 6 DAY AND t < current_date + INTERVAL 1 DAY\n);\n\n-- Día 1. Si el servicio tiene menos de 7, se repite el más reciente y el\n-- GROUP BY final colapsa el duplicado; nunca se pide una fecha inexistente.\nSET VARIABLE d1 = coalesce(getvariable('dias')[1], getvariable('dias')[1]);\nSET VARIABLE u1 = (SELECT '/vsicurl/https://services.geoglows.org/geoserver/satellite_based_precipitation/wcs'\n      || '?service=WCS&version=2.0.1&request=GetCoverage'\n      || '&coverageId=satellite_based_precipitation__' || $capa || '&format=image/tiff'\n      || '&subset=Long(' || round(ST_XMin(g), 3) || ',' || round(ST_XMax(g), 3) || ')'\n      || '&subset=Lat('  || round(ST_YMin(g), 3) || ',' || round(ST_YMax(g), 3) || ')'\n      || '&subset=time(%22' || strftime(d AT TIME ZONE 'UTC', '%Y-%m-%dT%H:%M:%S.000Z') || '%22)' FROM (SELECT ST_GeomFromText(getvariable('zona')) AS g, getvariable('d1') AS d));\n\n-- Día 2. Si el servicio tiene menos de 7, se repite el más reciente y el\n-- GROUP BY final colapsa el duplicado; nunca se pide una fecha inexistente.\nSET VARIABLE d2 = coalesce(getvariable('dias')[2], getvariable('dias')[1]);\nSET VARIABLE u2 = (SELECT '/vsicurl/https://services.geoglows.org/geoserver/satellite_based_precipitation/wcs'\n      || '?service=WCS&version=2.0.1&request=GetCoverage'\n      || '&coverageId=satellite_based_precipitation__' || $capa || '&format=image/tiff'\n      || '&subset=Long(' || round(ST_XMin(g), 3) || ',' || round(ST_XMax(g), 3) || ')'\n      || '&subset=Lat('  || round(ST_YMin(g), 3) || ',' || round(ST_YMax(g), 3) || ')'\n      || '&subset=time(%22' || strftime(d AT TIME ZONE 'UTC', '%Y-%m-%dT%H:%M:%S.000Z') || '%22)' FROM (SELECT ST_GeomFromText(getvariable('zona')) AS g, getvariable('d2') AS d));\n\n-- Día 3. Si el servicio tiene menos de 7, se repite el más reciente y el\n-- GROUP BY final colapsa el duplicado; nunca se pide una fecha inexistente.\nSET VARIABLE d3 = coalesce(getvariable('dias')[3], getvariable('dias')[1]);\nSET VARIABLE u3 = (SELECT '/vsicurl/https://services.geoglows.org/geoserver/satellite_based_precipitation/wcs'\n      || '?service=WCS&version=2.0.1&request=GetCoverage'\n      || '&coverageId=satellite_based_precipitation__' || $capa || '&format=image/tiff'\n      || '&subset=Long(' || round(ST_XMin(g), 3) || ',' || round(ST_XMax(g), 3) || ')'\n      || '&subset=Lat('  || round(ST_YMin(g), 3) || ',' || round(ST_YMax(g), 3) || ')'\n      || '&subset=time(%22' || strftime(d AT TIME ZONE 'UTC', '%Y-%m-%dT%H:%M:%S.000Z') || '%22)' FROM (SELECT ST_GeomFromText(getvariable('zona')) AS g, getvariable('d3') AS d));\n\n-- Día 4. Si el servicio tiene menos de 7, se repite el más reciente y el\n-- GROUP BY final colapsa el duplicado; nunca se pide una fecha inexistente.\nSET VARIABLE d4 = coalesce(getvariable('dias')[4], getvariable('dias')[1]);\nSET VARIABLE u4 = (SELECT '/vsicurl/https://services.geoglows.org/geoserver/satellite_based_precipitation/wcs'\n      || '?service=WCS&version=2.0.1&request=GetCoverage'\n      || '&coverageId=satellite_based_precipitation__' || $capa || '&format=image/tiff'\n      || '&subset=Long(' || round(ST_XMin(g), 3) || ',' || round(ST_XMax(g), 3) || ')'\n      || '&subset=Lat('  || round(ST_YMin(g), 3) || ',' || round(ST_YMax(g), 3) || ')'\n      || '&subset=time(%22' || strftime(d AT TIME ZONE 'UTC', '%Y-%m-%dT%H:%M:%S.000Z') || '%22)' FROM (SELECT ST_GeomFromText(getvariable('zona')) AS g, getvariable('d4') AS d));\n\n-- Día 5. Si el servicio tiene menos de 7, se repite el más reciente y el\n-- GROUP BY final colapsa el duplicado; nunca se pide una fecha inexistente.\nSET VARIABLE d5 = coalesce(getvariable('dias')[5], getvariable('dias')[1]);\nSET VARIABLE u5 = (SELECT '/vsicurl/https://services.geoglows.org/geoserver/satellite_based_precipitation/wcs'\n      || '?service=WCS&version=2.0.1&request=GetCoverage'\n      || '&coverageId=satellite_based_precipitation__' || $capa || '&format=image/tiff'\n      || '&subset=Long(' || round(ST_XMin(g), 3) || ',' || round(ST_XMax(g), 3) || ')'\n      || '&subset=Lat('  || round(ST_YMin(g), 3) || ',' || round(ST_YMax(g), 3) || ')'\n      || '&subset=time(%22' || strftime(d AT TIME ZONE 'UTC', '%Y-%m-%dT%H:%M:%S.000Z') || '%22)' FROM (SELECT ST_GeomFromText(getvariable('zona')) AS g, getvariable('d5') AS d));\n\n-- Día 6. Si el servicio tiene menos de 7, se repite el más reciente y el\n-- GROUP BY final colapsa el duplicado; nunca se pide una fecha inexistente.\nSET VARIABLE d6 = coalesce(getvariable('dias')[6], getvariable('dias')[1]);\nSET VARIABLE u6 = (SELECT '/vsicurl/https://services.geoglows.org/geoserver/satellite_based_precipitation/wcs'\n      || '?service=WCS&version=2.0.1&request=GetCoverage'\n      || '&coverageId=satellite_based_precipitation__' || $capa || '&format=image/tiff'\n      || '&subset=Long(' || round(ST_XMin(g), 3) || ',' || round(ST_XMax(g), 3) || ')'\n      || '&subset=Lat('  || round(ST_YMin(g), 3) || ',' || round(ST_YMax(g), 3) || ')'\n      || '&subset=time(%22' || strftime(d AT TIME ZONE 'UTC', '%Y-%m-%dT%H:%M:%S.000Z') || '%22)' FROM (SELECT ST_GeomFromText(getvariable('zona')) AS g, getvariable('d6') AS d));\n\n-- Día 7. Si el servicio tiene menos de 7, se repite el más reciente y el\n-- GROUP BY final colapsa el duplicado; nunca se pide una fecha inexistente.\nSET VARIABLE d7 = coalesce(getvariable('dias')[7], getvariable('dias')[1]);\nSET VARIABLE u7 = (SELECT '/vsicurl/https://services.geoglows.org/geoserver/satellite_based_precipitation/wcs'\n      || '?service=WCS&version=2.0.1&request=GetCoverage'\n      || '&coverageId=satellite_based_precipitation__' || $capa || '&format=image/tiff'\n      || '&subset=Long(' || round(ST_XMin(g), 3) || ',' || round(ST_XMax(g), 3) || ')'\n      || '&subset=Lat('  || round(ST_YMin(g), 3) || ',' || round(ST_YMax(g), 3) || ')'\n      || '&subset=time(%22' || strftime(d AT TIME ZONE 'UTC', '%Y-%m-%dT%H:%M:%S.000Z') || '%22)' FROM (SELECT ST_GeomFromText(getvariable('zona')) AS g, getvariable('d7') AS d));\n\nSELECT\n  t                        AS time,\n  round(max((s).mean), 3)  AS \"media mm\",\n  max((s).maximum)         AS \"máximo mm\"\nFROM (\n  SELECT getvariable('d1')::TIMESTAMPTZ AS t,\n         RT_CubeStats_Agg(RT_CubeClip(databand_1, tile_x, tile_y, metadata, ST_GeomFromText(getvariable('zona')), -99.0), 0) AS s\n  FROM RT_Read(getvariable('u1'))\n  UNION ALL\n  SELECT getvariable('d2')::TIMESTAMPTZ AS t,\n         RT_CubeStats_Agg(RT_CubeClip(databand_1, tile_x, tile_y, metadata, ST_GeomFromText(getvariable('zona')), -99.0), 0) AS s\n  FROM RT_Read(getvariable('u2'))\n  UNION ALL\n  SELECT getvariable('d3')::TIMESTAMPTZ AS t,\n         RT_CubeStats_Agg(RT_CubeClip(databand_1, tile_x, tile_y, metadata, ST_GeomFromText(getvariable('zona')), -99.0), 0) AS s\n  FROM RT_Read(getvariable('u3'))\n  UNION ALL\n  SELECT getvariable('d4')::TIMESTAMPTZ AS t,\n         RT_CubeStats_Agg(RT_CubeClip(databand_1, tile_x, tile_y, metadata, ST_GeomFromText(getvariable('zona')), -99.0), 0) AS s\n  FROM RT_Read(getvariable('u4'))\n  UNION ALL\n  SELECT getvariable('d5')::TIMESTAMPTZ AS t,\n         RT_CubeStats_Agg(RT_CubeClip(databand_1, tile_x, tile_y, metadata, ST_GeomFromText(getvariable('zona')), -99.0), 0) AS s\n  FROM RT_Read(getvariable('u5'))\n  UNION ALL\n  SELECT getvariable('d6')::TIMESTAMPTZ AS t,\n         RT_CubeStats_Agg(RT_CubeClip(databand_1, tile_x, tile_y, metadata, ST_GeomFromText(getvariable('zona')), -99.0), 0) AS s\n  FROM RT_Read(getvariable('u6'))\n  UNION ALL\n  SELECT getvariable('d7')::TIMESTAMPTZ AS t,\n         RT_CubeStats_Agg(RT_CubeClip(databand_1, tile_x, tile_y, metadata, ST_GeomFromText(getvariable('zona')), -99.0), 0) AS s\n  FROM RT_Read(getvariable('u7'))\n)\nWHERE t IS NOT NULL\nGROUP BY t\nORDER BY t;\n",
          "refId": "A"
        }
      ],
      "title": "Seven days over the area · mean and maximum",
      "fieldConfig": {
        "defaults": {
          "unit": "none",
          "decimals": 2,
          "custom": {
            "drawStyle": "line",
            "lineWidth": 2,
            "pointSize": 6,
            "showPoints": "always",
            "fillOpacity": 8,
            "spanNulls": true,
            "axisLabel": "media (mm)"
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "máximo mm"
            },
            "properties": [
              {
                "id": "custom.fillOpacity",
                "value": 0
              },
              {
                "id": "custom.lineStyle",
                "value": {
                  "fill": "dash",
                  "dash": [
                    8,
                    4
                  ]
                }
              },
              {
                "id": "custom.axisPlacement",
                "value": "right"
              },
              {
                "id": "custom.axisLabel",
                "value": "máximo (mm)"
              },
              {
                "id": "displayName",
                "value": "maximum mm"
              }
            ]
          }
        ]
      },
      "options": {
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true
        },
        "tooltip": {
          "mode": "multi",
          "sort": "none"
        }
      },
      "timeFrom": "7d"
    }
  ]
}
